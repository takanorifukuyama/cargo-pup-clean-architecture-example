[package]
name = "cargo-pup-clean-architecture-example"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"
description = "An executable Clean Architecture example with cargo-pup regression tests"

[lib]
name = "clean_architecture"

[[bin]]
name = "task-demo"
path = "src/main.rs"

# Deliberately dependency-free: the example demonstrates boundaries, not a framework.
